{% extends 'base.html.twig' %}

{% block title %}Edit my profile{% endblock %}

{% block body %}
    <h1 class="heading-padding">Edit my profile</h1>

<div class="container">

    {{ include('student/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('student_index') }}" class="btn btn-info">back to list</a>

    {{ include('student/_delete_form.html.twig') }}

</div>
{% endblock %}
